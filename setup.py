from setuptools import setup, find_packages

setup(
    name='lecoro',  # The name of your package
    version='0.1',  # Package version
    packages=find_packages(),
    description='Your description of the coro package',
    author='Your Name',
    author_email='your.email@example.com',
    url='https://github.com/yourusername/coro-ops',  # URL to the repository (if applicable)
    install_requires=[],  # Add dependencies from requirements.txt if needed
)
